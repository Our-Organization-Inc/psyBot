<div class="nav">
  <div class="title">
    PsyBot
  </div>
  <div class="nav-body">
    <ul>
      <li><a href="/home">Home</a></li>
    </ul>
  </div>
  <div class="nav-end">
    <ul>
      <li><a href="{{s_or_lg}}">{{s_or_l}}</a></li>
    </ul>
  </div>
</div>
<div>
  {{#if usrname}}
  Logged in as {{usrname}}
  {{/if}}
  {{#else}}
  Log in to <a href="/login">continue</a>
  {{/else}}
</div>